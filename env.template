# Supabase public client keys (used by Vite frontend)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key

# Server-side only secrets (never expose to browser bundles)
# SUPABASE_URL is optional if VITE_SUPABASE_URL is set (API will fallback)
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# CLI / CI usage (optional)
SUPABASE_PROJECT_REF=your-project-ref
SUPABASE_ACCESS_TOKEN=your-supabase-access-token

# Add additional environment variables below as needed

# Stripe payment gateway
VITE_STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key
STRIPE_SECRET_KEY=your-stripe-secret-key
STRIPE_WEBHOOK_SECRET=your-stripe-webhook-secret
# STRIPE_PAYMENT_CURRENCY=aud  # Optional, defaults to "aud"

# Unsplash API
# Generate from https://unsplash.com/developers
UNSPLASH_ACCESS_KEY=your-unsplash-access-key

# Resend Email Service
# Generate from https://resend.com/api-keys
RESEND_API_KEY=your-resend-api-key
RESEND_FROM_EMAIL="MACMAA <onboarding@resend.dev>"
ADMIN_NOTIFICATION_EMAIL=admin@macmaa.org

# Test Email (用于测试邮件发送功能)
# 可以通过 /api/test-email 端点测试邮件功能
# 如果不提供 'to' 字段，将使用此邮箱作为收件人
TEST_EMAIL=your-test-email@example.com

